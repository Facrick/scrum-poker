# Скопируйте в .env и заполните перед деплоем
# cp .env.example .env

# Домен, с которого разрешены запросы (без слеша в конце)
ALLOWED_ORIGIN=https://poker.yourcompany.com

# Через сколько часов удалять неактивные комнаты (по умолчанию 8)
ROOM_TTL_HOURS=8
